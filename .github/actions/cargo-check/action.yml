name: cargo check
description: Run cargo check on the supplied scope.

inputs:
  scope:
    description: "Cargo scope flags (e.g. '-p anomstream-core --no-default-features')."
    required: true

runs:
  using: composite
  steps:
    - shell: bash
      env:
        SCOPE: ${{ inputs.scope }}
      run: cargo check $SCOPE
